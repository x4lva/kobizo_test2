<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="#"><i class="far fa-newspaper"></i></a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent"
                aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link {% if app.request.get('_route') == 'home' %}active{% endif %}" href="{{ path('home') }}">Home</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link {% if app.user==false %}disabled{% endif %} {% if app.request.get('_route') == 'post_list' %}active{% endif %}"
                       href="{{ path('post_list') }}">Post
                        list</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link {% if app.user==false %}disabled{% endif %} {% if app.request.get('_route') == 'post_create' %}active{% endif %}" href="{{ path('post_create') }}">Post create</a>
                </li>
            </ul>
            <form class="d-flex">
                {% if app.user %}
                    <a href="{{ path('app_logout') }}" class="btn btn-outline-primary" type="submit">Logout</a>
                {% else %}
                    <a href="{{ path('app_login') }}" class="btn btn-outline-success" type="submit">Login</a>
                {% endif %}
            </form>
        </div>
    </div>
</nav>